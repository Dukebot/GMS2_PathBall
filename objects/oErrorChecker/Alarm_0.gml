if instance_number(oGame) > 1
	exception("More than one oGame");
	
if instance_number(oInput) > 1 
	exception("More than one oInput");
	
if instance_number(oPauseButton) > 1
	exception("More than one pause button");
	
alarm[0] = 60;